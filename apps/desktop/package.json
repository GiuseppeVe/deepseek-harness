{
  "name": "@deepseek-ai/dsh-desktop",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cargo build --manifest-path src-tauri/Cargo.toml --bin dsh-desktop && vitest run --config vitest.config.ts tests/private-state.spec.ts"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}
