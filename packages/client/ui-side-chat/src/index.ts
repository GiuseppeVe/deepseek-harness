/**
 * Node half of ui-side-chat: browser-only surface; exists so the loader can
 * resolve the package root. @module @deepseek-ai/dsh-client-ui-side-chat
 */
export const name = 'client-ui-side-chat'

export function apply(_ctx: unknown): void { /* browser-only */ }